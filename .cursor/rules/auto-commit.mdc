---
description: 完成仓库改动后自动 git 提交并推送
alwaysApply: true
---

# 自动提交

在本仓库完成有意义的改动后，**无需用户再次确认**，自动执行 git 提交并推送到远程。

## 流程

1. `git status` / `git diff` 确认有可提交内容；无改动则跳过。
2. `git add` 相关文件（勿提交密钥、`.env` 等敏感文件）。
3. `git commit`：1–2 句，说明「为什么」；风格与现有提交一致。
4. `git push` 到 `origin` 当前分支（默认 `main`）。

## 约束

- 不修改 git config；不使用 `--no-verify`、`--force` 等破坏性选项。
- 提交失败（如 hook 拒绝）时修复问题后**新建提交**，不要 amend 已推送的提交。
- 用户明确要求「只提交不推送」时，跳过 push。
