# 条款 1：分清指针（*）和引用（&）的本质区别、使用场景边界

## 本节讲什么

> 待补充详细笔记（错误案例、原理、正确写法、代码示例）。

## 示例

```cpp
int x = 42;
int &rx = x;   // 引用必须绑定对象
int *px = &x;  // 指针可为空
// void f(int &r); f(*px);  // 错误：不能 *px 当初始化引用的对象链
```
