# 3.3 标准库类型 vector

## 本节讲什么

介绍 C++ 标准库中最常用的**容器**类型 **`std::vector`**——存放**同类型元素**的**动态数组**（可变长序列）。讲解定义、列表初始化、**`push_back`** 动态增容，以及下标访问规则（**不能用下标越界添加元素**）。

## 名字别搞混：vector ≠ 宿主

| 英文 | 常见中文 | 在 C++ / HFT 里指什么 |
|------|----------|------------------------|
| **vector** | **向量、容器** | `std::vector<T>`：标准库动态数组，本章主题 |
| **host** | **宿主** | 网络/系统语境：本机、宿主机（如 host byte order、宿主机 NIC） |

`vector` 来自数学「向量」的命名，在 C++ 里语义是**一维元素序列容器**，和网络里的「宿主机器」毫无关系。

## 要点

- `#include <vector>`；`std::vector<T>` 存放类型 `T` 的对象序列（**元素类型必须相同**）
- **动态数组**：长度不固定，元素在内存中**连续存放**（对缓存友好；HFT 批量 tick 常用）
- **定义**：`vector<int> v;`、`vector<int> v(10);`（10 个默认值）、`vector<int> v{1,2,3};`（列表初始化）
- **`push_back`**：在末尾追加元素，是运行时**增容、添加新元素**的正确方式
- **下标 `v[i]`**：仅用于访问**已存在**的元素（`0 <= i < size()`）；`v[v.size()]` 已越界
- **不能用下标添加**：`v[10] = 42;` 在 `size() <= 10` 时未定义行为——容器不会自动扩容，只是往非法地址写
- 可用 `size()`、`empty()` 查询状态；增删改查 API 见第 9 章顺序容器

## HFT：越界写 = 静默灾难

行情处理热路径里，`vector` 误用下标「添加」元素会导致**缓冲区外写（OOB write）**：

- **不一定会立刻 crash**：可能踩坏相邻对象、计数器、指针，表现为偶发错价、漏单
- **一旦崩溃往往在最坏时刻**：GC 压力、锁竞争、网络突发时，直接**错过交易窗口**
- **工程习惯**：
  - 新元素一律 **`push_back` / `emplace_back`**
  - 下标前断言或边界检查：`i < v.size()`
  - 容量可预估时用 **`reserve(n)`** 减少重分配（见 [3.2 string 的 reserve 思路](./3.2-标准库类型string.md)）

```cpp
// 行情缓冲：错误 vs 正确
std::vector<Tick> ticks;
// ticks[ticks.size()] = tick;   // ❌ 越界写，UB
ticks.push_back(tick);           // ✅
// ticks.emplace_back(/* ... */); // ✅ 原地构造，少一次拷贝
```

## 示例

```cpp
#include <iostream>
#include <vector>

int main() {
    std::vector<int> v;              // 空 vector
    for (int i = 1; i <= 5; ++i)
        v.push_back(i);              // 正确：动态添加元素

    std::vector<int> v2{1, 2, 3};    // 列表初始化

    for (std::size_t i = 0; i != v.size(); ++i)
        std::cout << v[i] << " ";    // 下标访问已有元素
    std::cout << std::endl;

    // 错误示例（不要这样做）：
    // v[10] = 100;  // 越界！不能用下标添加新元素

    return 0;
}
```
