# 1.6 书店程序

## 本节讲什么

本章总结性实战：结合输入输出、控制流与 `Sales_item` 类，编写书店程序——读取多条销售记录，汇总输出各本书总销售额和平均售价。

## 示例

```cpp
#include <iostream>
#define DEBUG_MODE 1
#if DEBUG_MODE
    #define LOG(x) std::cerr << x << std::endl
#else
    #define LOG(x)
#endif
int main() {
    LOG("compiled with debug");
    return 0;
}
```
