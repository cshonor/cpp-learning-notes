# 1.2 初识输入输出

## 本节讲什么

C++ 通过标准库中的 `iostream` 提供 IO 机制，其中包含表示输入流的 `istream` 和表示输出流的 `ostream` 基础类型。讲解如何使用 `std::cin` 与输入运算符 `>>` 读取数据，使用 `std::cout` 与输出运算符 `<<` 打印消息，使用 `std::cerr` 输出错误信息，以及使用操纵符 `std::endl` 结束当前行并刷新缓冲区。

## 要点

- 标准库头文件 `<iostream>` 提供 IO 支持
- `istream` / `ostream`：输入流与输出流的基础类型
- `std::cin` + `>>`：从标准输入读取
- `std::cout` + `<<`：向标准输出写入
- `std::cerr`：标准错误输出，常用于警告和错误信息
- `std::endl`：输出换行并刷新缓冲区

## 示例

```cpp
#include <iostream>
int main() {
    std::cout << "Enter two numbers: ";
    int v1 = 0, v2 = 0;
    std::cin >> v1 >> v2;
    std::cout << v1 << " + " << v2 << " = " << v1 + v2 << std::endl;
    return 0;
}
```
