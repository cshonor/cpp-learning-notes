# 小结

## 本节讲什么

本章涵盖 C++ 底层数据表示与类型管理的基础机制，是后续学习类与高级特性的先决条件。

## 示例

```cpp
#include <iostream>
int main() {
    // 第 2 章回顾：引用与指针
    int x = 10;
    int &rx = x;
    int *px = &x;
    rx = 20;
    std::cout << *px << std::endl;
    return 0;
}
```
