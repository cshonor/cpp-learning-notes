# 17.5 计时工具（chrono）

## 本节讲什么

常用：统计代码耗时、定时、时间戳转换。

## 要点

| 组件 | 说明 |
|------|------|
| 时钟 | `system_clock`（墙上时钟）、`steady_clock`（单调计时）、`high_resolution_clock` |
| 时长 | `duration`：时分秒毫秒纳秒单位转换 |
| 时间点 | `time_point` |
