# 4.12 运算符优先级表

## 本节讲什么

总结所有运算符的优先级和结合律。

## 示例

```cpp
#include <iostream>
int main() {
    // 优先级：* 高于 +
    int a = 2, b = 3;
    std::cout << a + b * 4 << std::endl;  // 14，不是 20
    return 0;
}
```
