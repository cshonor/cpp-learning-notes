# 3.1 语句与程序块

## 本节讲什么

C 程序由 **语句** 组成；多条语句可用花括号 `{}` **打包** 成一条 **复合语句（块）**。块既是 `if`、循环、函数体里「管多条语句」的手段，也定义 **块作用域** —— 块内声明的变量，出了 `{}` 就不能再用。

**后续**：[3.2 if-else](./3.2-if-else语句.md)、[1.10 作用域](../ch01-introduction/1.10-外部变量与作用域.md)、[2.4 声明](../ch02-types-operators-expressions/2.4-声明.md)。

## 要点

### 1. 语句与分号

- 多数语句以 **`;`** 结束：表达式语句、声明、空语句 `;`
- **控制流**（`if`、`for`、`while` 等）按各自语法，不都在末尾加单独分号代表「整条控制流结束」

### 2. 复合语句：把多条语句「打包」成一条

```c
{
    printf("A\n");
    printf("B\n");
}
```

**语法上 `{ ... }` 整体算一条语句**，因此可以写在只允许 **一条** 子语句的位置（`if`、`else`、`for` 体等）。

| 概念 | 说明 |
|------|------|
| **复合语句** | `{ 声明; 语句; ... }` |
| **作用** | 把多条语句合成 **一条**，交给 `if` / 循环管辖 |
| **无 `{}`** | 控制流只能管 **紧挨着的那一条** 语句 |

### 3. `if` 不加花括号：只管紧接着的一条

```c
if (score >= 60)
    printf("及格\n");
    printf("发证书\n");   /* 不在 if 里！无论真假都会执行 */
```

等价于：

```c
if (score >= 60)
    printf("及格\n");
printf("发证书\n");        /* 已脱离 if */
```

**加花括号**后，两条都在分支内：

```c
if (score >= 60) {
    printf("及格\n");
    printf("发证书\n");   /* 只有及格才执行 */
}
```

| | 无 `{}` | 有 `{}` |
|---|---------|---------|
| `if` 管辖 | **下一条语句 only** | 块内 **全部** |
| 第二条语句 | 易 **误以为** 在分支里 | 明确在分支里 |

**记忆**：`if` 没括号 → 只搂住下一个「单身」语句；要多条一起管 → **必须 `{}` 打成一包**。

### 4. 块作用域：出了 `{}` 变量不可用

块内 **声明** 的标识符，从声明点到 **`}`** 可见；离开块后 **名字失效**（对象销毁，见 [1.10](../ch01-introduction/1.10-外部变量与作用域.md) 生命周期）。

```c
{
    int x = 1;
    printf("%d\n", x);
}
/* x 在此不可见，不可再使用 */

if (n > 0) {
    int i;
    for (i = 0; i < n; i++)
        /* ... */
}  /* i 仅在此 if 块内有效 */
```

| 项 | 说明 |
|----|------|
| **可见性** | 块内声明 → 块外 **不能引用** |
| **生命周期** | 进入块创建（若未 `static`），离开 `}` 销毁（自动存储） |
| **隔离** | 不同块可同名变量，互不影响 |

### 5. C89 vs C99 声明位置

| 标准 | 块内声明 |
|------|----------|
| **C89** | 须在块 **第一条语句之前** |
| **C99** | **用到再声明**；`for (int i = 0; …)` 合法（[2.1.1 C99](../ch02-types-operators-expressions/2.1.1-C99标准与演进.md)） |

### 6. 单独 `{}` 限制临时变量

```c
while (read_pkt(&hdr) == 0) {
    {
        uint32_t len = ntohl(hdr.len);  /* 仅本迭代需要 */
        if (len > MAX) break;
        process(len);
    }
}
```

热路径用块 **缩小变量作用域**，利优化与审查。

## HFT / 底层示例

```c
if (pkt_len < HDR_SZ)
    return -1;
    stats.drop++;   /* 坑：不在 if 内，每次循环都可能执行 */

/* 正确 */
if (pkt_len < HDR_SZ) {
    stats.drop++;
    return -1;
}
```

## 与后续衔接

- **函数体** 本身就是一个块
- **3.2–3.8**：`if`、循环、`switch` 的子语句都依赖本节「一条 vs 复合语句」
- **ch04**：`auto` 存储、完整作用域规则

## 面试题

1. `if (f()) g(); h();` 中 `h()` 是否受 `if` 控制？
2. 复合语句在语法上算几条语句？为何 `if` 后面可以跟 `{ ... }`？
3. 块内 `int i` 在块外能否使用？何时销毁？
4. 为何生产代码推荐 `if` 分支 **始终加 `{}`**？
