# 2.2 数据类型及长度

## 本节讲什么

内置类型的 **存储宽度** 与 **有符号/无符号** 语义——理解内存、寄存器、协议字段、ABI 对齐的基础。

**核心结论**：

1. **标准 C 只规定最小取值范围与相对大小**，**不固定**各类型字节数
2. 实际占用字节、数值上下限，由 **CPU 架构（32/64 位 x86、ARM）+ 编译器 + OS ABI** 决定

与 [1.2 变量与算术表达式](../ch01-introduction/1.2-变量与算术表达式.md) 的 `int`/`float` 衔接；与 [C 陷阱 ch07 7.1](../../03-C-Traps-and-Pitfalls/ch07-portability-pitfalls/7.1-整型宽度.md) 可移植性专题对照。

## 要点

### 1. 基础类型与 CPU 的关联

| 类型 | 典型字节 | 受 CPU 影响 | 说明 |
|------|----------|-------------|------|
| **`char`** | **1** | 极小 | 最小可寻址单元；有符号 `-128~127` 或 无符号 `0~255`（**实现定义** signed/unsigned） |
| **`short`** | **≥2**，通常 **2** | 很小 | 标准要求 `sizeof(short) ≥ sizeof(char)` |
| **`int`** | **≥2**，现代几乎 **4** | **较大** | 32 位 CPU：通常 4；64 位 x86/ARM：**多数仍为 4**；老 16 位机曾见 2 字节 |
| **`long`** | **≥4** | **很大** | 32 位：**4**；Win64：**4**（LLP64）；Linux/macOS 64 位 LP64：**8** |
| **`long long`** | **≥8**，通常 **8** | 小 | C99 起；32/64 位下普遍 8 字节 |
| **`float` / `double`** | **4 / 8** | 现代 x86/ARM 上较统一 | IEEE 754 单/双精度 |
| **`void *`** | **4 或 8** | **等于 CPU 位宽** | 32 位 4 字节；64 位 8 字节 |

标准只保证：`char ≤ short ≤ int ≤ long ≤ long long`，且 **`sizeof(char) == 1`**。

### 2. 有符号 / 无符号范围由字节长度推导

字节越多，可表示范围越大（有符号补码 / 无符号模 \(2^n\)）：

| 宽度 | 有符号 `int` 量级 | 无符号量级 |
|------|-------------------|------------|
| 4 字节 | 约 ±21 亿 | 约 42 亿 |
| 8 字节 | 约 ±9×10¹⁸ | 约 1.8×10¹⁹ |

CPU 寄存器宽度影响编译器对「自然整型」的选择，这是 **`int`/`long` 随平台变化** 的根源。

### 3. `sizeof` 与 `<limits.h>`

```c
#include <stdio.h>

int main(void)
{
    printf("sizeof char=%zu short=%zu int=%zu long=%zu long long=%zu void*=%zu\n",
           sizeof(char), sizeof(short), sizeof(int),
           sizeof(long), sizeof(long long), sizeof(void *));
    return 0;
}
```

- **`sizeof(T)`**：编译期常量，类型 **字节数**
- **`<limits.h>`**：`INT_MAX`、`LONG_MAX` 等 **实现定义** 上下限
- 打印 `sizeof` 用 **`%zu`**（`size_t`），不要用 `%d`

### 4. 直观例子：同一 `long`，不同平台

```c
printf("long size: %zu\n", sizeof(long));
```

| 环境 | 典型输出 |
|------|----------|
| 32 位 Linux | **4** |
| Windows 64 位（LLP64） | **4** |
| 64 位 Ubuntu / WSL（LP64） | **8** |

同一份源码，**`long` 能表示的范围完全不同**——跨平台用裸 `long` 存时间戳、大计数易溢出或截断。

### 5. 固定宽度整数：`stdint.h`（嵌入式 / HFT 必用）

为消除架构差异，C99 提供 **定长类型**：

```c
#include <stdint.h>

uint8_t   u8;
int16_t   i16;
uint32_t  u32;
int64_t   i64;
```

| 类型 | 保证 |
|------|------|
| `int32_t` / `uint32_t` | 恰好 32 位 |
| `int64_t` / `uint64_t` | 恰好 64 位 |

不管 32 还是 64 位 CPU，**wire format、寄存器镜像、跨进程协议** 应优先用这套；`size_t` / `ptrdiff_t` 用于大小与指针差。

### 6. HFT / 嵌入式实用总结

1. 普通 **`int` / `long` 有平台差异**，底层高性能代码勿假设固定宽度
2. **一律用 `<stdint.h>` 定长类型**，屏蔽 CPU/ABI 差异
3. **`float`/`double`** 在现代 x86/ARM64 上通常 4/8 字节，但仍勿用 `==` 比浮点
4. **指针宽度 = CPU 位宽**；指针与整数互转用 **`uintptr_t`**，不用 `int`（见 C 陷阱 7.5）
5. 字节数据用 **`unsigned char` / `uint8_t`**，不裸用可能 signed 的 `char`

## 与 C++ 的差异

| 主题 | C++ | C |
|------|-----|---|
| 定宽类型 | `<cstdint>` 同 `stdint.h` | `<stdint.h>` |
| `char` 默认符号 | 实现定义 | 实现定义 |
| `long` 宽度 | 同 C，随平台变 | 同左 |

## Demo

```bash
cd demo/demo01_sizeof && make && ./main
```

## 与后续衔接

- **2.7 类型转换**：有符号/无符号混算、整型提升
- **2.9 按位运算**：宽度决定掩码范围
- **ch05 指针**：`sizeof` 与数组/指针对照
- **ch07 可移植性**：字节序、对齐、LP64/LLP64

## 面试题

1. 为何 64 位 Linux 上 `int` 常仍是 4 字节，而 `long` 是 8？
2. Windows 64 与 Linux 64 的 `long` 为何可能不同？
3. 协议字段定 32 位无符号整数，应 typedef 成什么？
