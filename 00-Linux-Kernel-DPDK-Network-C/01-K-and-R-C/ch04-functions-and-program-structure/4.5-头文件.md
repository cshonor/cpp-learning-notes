# 4.5 头文件

## 本节讲什么

**头文件**（后缀 **`.h`**）专门放 **声明** 和可共享的 **类型/宏**，供多个 `.c` 通过 **`#include`** 复用。多文件工程的标准做法：每个 `.c` 单独编译成 `.o`，链接器合并；**`.h` 保证大家用的是同一套声明**。

**前置**：[4.1 函数原型](./4.1-函数的基本知识.md)、[4.3 外部变量](./4.3-外部变量.md)、[4.4 作用域与链接](./4.4-作用域规则.md)。

## 要点

### 1. 头文件是什么

**头文件** = 后缀为 **`.h`** 的文件，**专门用来放声明**（以及宏、类型定义），**一般不写函数体、不写全局变量定义**。

| 常放内容 | 示例 |
|----------|------|
| **变量的 `extern` 声明** | `extern int packet_count;` |
| **函数原型** | `int add(int a, int b);` |
| **宏定义** | `#define MAXLINE 1000` |
| **结构体 / 类型定义** | `struct node { ... };`、`typedef int bool_t;` |

**不放**（应放在 **某一个 `.c`**）：

- 全局变量 **定义** `int num = 0;`
- 函数 **实现** `{ ... }`
- `static` 模块私有符号（只在本 `.c`）

### 2. 为什么需要头文件：`#include` 复用声明

当 **多个 `.c`** 都要用 **同一组声明** 时：

- **不好**：每个 `.c` 手写一遍原型 → 易抄错、改一处漏改别处
- **好**：写在 **一个 `.h`**，各 `.c` **`#include`** 进来

```c
/* math_utils.h */
#ifndef MATH_UTILS_H
#define MATH_UTILS_H

extern int g_seed;
int add(int a, int b);
double square(double x);

#endif
```

```c
/* main.c */
#include "math_utils.h"

int main(void)
{
    int s = add(1, 2);
    return 0;
}
```

```c
/* math_utils.c */
#include "math_utils.h"

int g_seed = 0;              /* 唯一定义 */

int add(int a, int b)
{
    return a + b;
}

double square(double x)
{
    return x * x;
}
```

**好处**：

1. **避免重复写** 相同声明
2. **保证一致** —— 所有 `.c` include 同一份 `.h`，编译器看到的 **函数签名、类型、宏** 相同
3. **接口清晰** —— `.h` = 模块对外「菜单」，`.c` = 实现

预处理时 `#include "math_utils.h"` 等价于把 `.h` 内容 **粘贴** 进当前 `.c` 再编译（[4.11 预处理器](./4.11-c-preprocessor/4.11.1-文件包含.md)）。

### 2.1 完整例子：`global_num`（a.c + common.h + b.c / c.c）

**① 在 `a.c` 里定义（全程序唯一定义）**

```c
/* a.c */
int global_num = 100;
```

**② 建 `common.h`，只写 `extern` 声明**

```c
/* common.h */
#ifndef COMMON_H
#define COMMON_H

extern int global_num;

#endif
```

**③ `b.c`、`c.c` 包含头文件即可用，不必再手写 `extern`**

```c
/* b.c */
#include <stdio.h>
#include "common.h"

void show_in_b(void)
{
    printf("%d\n", global_num);   /* 输出 100；头文件已带 extern 声明 */
}
```

```c
/* c.c */
#include "common.h"

void double_in_c(void)
{
    global_num *= 2;              /* 改的是 a.c 定义的那份对象 */
}
```

```bash
gcc a.c b.c c.c -o demo
```

| 文件 | 写什么 |
|------|--------|
| **`a.c`** | `int global_num = 100;` —— **定义** |
| **`common.h`** | `extern int global_num;` —— **声明** |
| **`b.c` / `c.c`** | `#include "common.h"` —— **自动** 带上声明，无需再写 `extern int global_num;` |

**改类型时如何同步**：若要把 `global_num` 从 `int` 改成 `float`：

1. **`a.c`** 改定义：`float global_num = 100.0f;`
2. **`common.h`** 改声明：`extern float global_num;`
3. 所有 `#include "common.h"` 的 `.c` **自动** 看到新类型 —— **不会出现** b.c 还当 `int`、a.c 已是 `float` 的 **声明/定义不一致**

若不用头文件、各 `.c` 自己抄 `extern`，极易漏改某一文件 → 链接通过或静默错行为。**头文件 = 单一真相来源**。

### 3. 编译链接流程

```
a.c  ──→ a.o  ─┐
b.c  ──→ b.o  ─┼──→ 链接器 ──→ a.out
```

- **编译单元** = **一个 `.c` 文件**（`#include` 展开后）单独编译 → **`.o`**
- **链接器** 合并 `.o`；**外部链接** 符号跨文件合并（[4.4 §3](./4.4-作用域规则.md)）

### 4. 单定义规则：`.h` 只声明，`.c` 定义

| 放在 `.h` | 放在 **某一个** `.c` |
|-----------|---------------------|
| `extern int num;` | `int num = 0;` |
| `int foo(int x);` | `int foo(int x) { return x; }` |
| `struct point { int x, y; };` | （类型定义可放 `.h`；带静态数据的定义放 `.c`） |

- 每个 **外部链接** 符号全程序 **只能定义一次**
- **`.h` 被多个 `.c` include** 时，写 `int num = 0;` → 每个 `.c` 各一份定义 → 链接 **重复定义**
- 正确：`.h` 写 **`extern int num;`**，**一个** `.c` 写 **`int num = 0;`**

见 [4.3](./4.3-外部变量.md)。

### 5. include 守卫（防止重复展开）

```c
#ifndef MATH_UTILS_H
#define MATH_UTILS_H
/* 头文件内容 */
#endif
```

同一 `.c` 多次 include 或链式 include 时，避免重复定义类型/宏。

### 6. `#include "..."` vs `#include <...>`

| 写法 | 查找路径 | 典型 |
|------|----------|------|
| **`#include "foo.h"`** | **当前目录**、工程路径优先 | 项目自己的 `.h` |
| **`#include <stdio.h>`** | **系统/编译器** 标准路径 | 标准库、第三方库 |

## 示例：栈模块（K&R 风格）

```c
/* stack.h —— 声明集中处 */
#ifndef STACK_H
#define STACK_H

#define MAXVAL 100
void push(double f);
double pop(void);

#endif
```

```c
/* stack.c —— 实现 + 本文件私有 */
#include "stack.h"

static int sp = 0;           /* static：不放进 .h，仅本 .c */
static double val[MAXVAL];

void push(double f) { /* ... */ }
double pop(void)    { /* ... */ }
```

```c
/* main.c */
#include "stack.h"

int main(void)
{
    push(1.0);
    return 0;
}
```

## 易混点

| # | 要点 |
|---|------|
| 1 | **`.h` = 声明**；**`.c` = 定义/实现** |
| 2 | 多 `.c` 共用声明 → 写 **一个 `.h`** + **`#include`** |
| 3 | `.h` 里 **`extern` 声明**，**一个** `.c` **定义** |
| 4 | `.h` 里写 `int x = 0;` → 多 include → **链接炸** |
| 5 | **`static` 私有** 放 `.c`，**不要** 写进对外 `.h` |
| 6 | include 守卫防重复展开 |

## HFT / 内核提示

- 驱动：**`xxx.h` 对外 API**，**`xxx.c` 实现** + `static` 私有
- 策略引擎：公共类型/错误码放 `.h`，热路径实现藏 `.c`
- 改 `.h` 中结构体布局 → **所有 include 的 `.c` 重编**（接口稳定性）

## 与后续衔接

| 章节 | 内容 |
|------|------|
| **4.8** | 逆波兰计算器多 `.c` + 各 `.h` |
| **4.11** | `#include` 预处理 |
| **ch06** | 结构体定义常放 `.h` |

## 面试题

1. 头文件主要放什么？为什么不把 `int num = 0;` 定义放 `.h`？
2. `b.c` 只 `#include "common.h"` 后能否直接 `printf("%d", global_num);`？改类型要改几处？
3. `extern` 声明为什么适合放 `.h`？
4. `static` 函数/全局应放在 `.h` 还是 `.c`？为什么？
5. `#include "x.h"` 与 `#include <x.h>` 有何区别？
