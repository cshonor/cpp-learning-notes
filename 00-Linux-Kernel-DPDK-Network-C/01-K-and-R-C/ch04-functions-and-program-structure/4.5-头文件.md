# 4.5 头文件

## 本节讲什么

**头文件**（后缀 **`.h`**）专门放 **声明** 和可共享的 **类型/宏**，供多个 `.c` 通过 **`#include`** 复用。多文件工程的标准做法：每个 `.c` 单独编译成 `.o`，链接器合并；**`.h` 保证大家用的是同一套声明**。

**前置**：[4.1 函数原型](./4.1-函数的基本知识.md)、[4.3 外部变量](./4.3-外部变量.md)、[4.4 作用域与链接](./4.4-作用域规则.md)。

## 要点

### 1. 头文件是什么

**头文件** = 后缀为 **`.h`** 的文件，**专门用来放声明**（以及宏、类型定义），**一般不写函数体、不写全局变量定义**。

| 常放内容 | 示例 |
|----------|------|
| **变量的 `extern` 声明** | `extern int packet_count;` |
| **函数原型** | `int add(int a, int b);` |
| **宏定义** | `#define MAXLINE 1000` |
| **结构体 / 类型定义** | `struct node { ... };`、`typedef int bool_t;` |

**不放**（应放在 **某一个 `.c`**）：

- 全局变量 **定义** `int num = 0;`
- 函数 **实现** `{ ... }`
- `static` 模块私有符号（只在本 `.c`）

### 2. 为什么需要头文件：`#include` 复用声明

当 **多个 `.c`** 都要用 **同一组声明** 时：

- **不好**：每个 `.c` 手写一遍原型 → 易抄错、改一处漏改别处
- **好**：写在 **一个 `.h`**，各 `.c` **`#include`** 进来

```c
/* math_utils.h */
#ifndef MATH_UTILS_H
#define MATH_UTILS_H

extern int g_seed;
int add(int a, int b);
double square(double x);

#endif
```

```c
/* main.c */
#include "math_utils.h"

int main(void)
{
    int s = add(1, 2);
    return 0;
}
```

```c
/* math_utils.c */
#include "math_utils.h"

int g_seed = 0;              /* 唯一定义 */

int add(int a, int b)
{
    return a + b;
}

double square(double x)
{
    return x * x;
}
```

**好处**：

1. **避免重复写** 相同声明
2. **保证一致** —— 所有 `.c` include 同一份 `.h`，编译器看到的 **函数签名、类型、宏** 相同
3. **接口清晰** —— `.h` = 模块对外「菜单」，`.c` = 实现

预处理时 `#include "math_utils.h"` 等价于把 `.h` 内容 **粘贴** 进当前 `.c` 再编译（[4.11 预处理器](./4.11-c-preprocessor/4.11.1-文件包含.md)）。

### 2.1 完整例子：`global_num`（a.c + common.h + b.c / c.c）

**① 在 `a.c` 里定义（全程序唯一定义）**

```c
/* a.c */
int global_num = 100;
```

**② 建 `common.h`，只写 `extern` 声明**

```c
/* common.h */
#ifndef COMMON_H
#define COMMON_H

extern int global_num;

#endif
```

**③ `b.c`、`c.c` 包含头文件即可用，不必再手写 `extern`**

```c
/* b.c */
#include <stdio.h>
#include "common.h"

void show_in_b(void)
{
    printf("%d\n", global_num);   /* 输出 100；头文件已带 extern 声明 */
}
```

```c
/* c.c */
#include "common.h"

void double_in_c(void)
{
    global_num *= 2;              /* 改的是 a.c 定义的那份对象 */
}
```

```bash
gcc a.c b.c c.c -o demo
```

| 文件 | 写什么 |
|------|--------|
| **`a.c`** | `int global_num = 100;` —— **定义** |
| **`common.h`** | `extern int global_num;` —— **声明** |
| **`b.c` / `c.c`** | `#include "common.h"` —— **自动** 带上声明，无需再写 `extern int global_num;` |

**改类型时如何同步**：若要把 `global_num` 从 `int` 改成 `float`：

1. **`a.c`** 改定义：`float global_num = 100.0f;`
2. **`common.h`** 改声明：`extern float global_num;`
3. 所有 `#include "common.h"` 的 `.c` **自动** 看到新类型 —— **不会出现** b.c 还当 `int`、a.c 已是 `float` 的 **声明/定义不一致**

若不用头文件、各 `.c` 自己抄 `extern`，极易漏改某一文件 → 链接通过或静默错行为。**头文件 = 单一真相来源**。

### 3. 编译链接流程：编译「认声明」，链接「绑定义」

```
a.c  ──→ a.o  ─┐
b.c  ──→ b.o  ─┼──→ 链接器 ──→ a.out
c.c  ──→ c.o  ─┘
```

**编译阶段**（每个 `.c` **单独** 编译成 `.o`）：

| 文件 | 编译器看到什么 | 结果 |
|------|----------------|------|
| **`a.c`** | `int global_num = 100;` —— **定义** | `a.o` 里 **导出** 符号 `global_num`（有实体、占内存） |
| **`b.c`** | `#include "common.h"` → `extern int global_num;` —— **仅声明** | `b.o` 里 **引用** 符号 `global_num`（知道「有这么个 `int`」，**不知道** 定义在哪） |
| **`c.c`** | 同上 | `c.o` 里同样是 **未解析的引用** |

要点：**`b.c` 包含 `common.h` 后，编译器只确认「存在一个外部链接的 `global_num`」**，类型检查、`printf("%d", global_num)` 都能过；**此时不必、也无法** 知道它在哪个 `.c` 定义 —— 那是 **链接器** 的工作。

**链接阶段**（链接器汇总 **所有** `.o` 里的符号表）：

1. 在 **`a.o`** 找到 **`global_num` 的定义**（分配好的那一份内存）
2. 把 **`b.o`、`c.o`** 里对 `global_num` 的 **引用** 填成 **同一地址**
3. 生成最终可执行文件 —— 三文件访问的是 **同一块内存**

```
a.o:  global_num  ← 定义（实体）
b.o:  global_num  ← 引用 ──┐
c.o:  global_num  ← 引用 ──┼── 链接器绑定到 a.o 那份
```

若 **没有任何 `.o` 提供定义** → **`undefined reference to 'global_num'`**（未定义引用）。  
若 **多个 `.o` 都有定义** → **`multiple definition of 'global_num'`**（重复定义）。

- **编译单元** = **一个 `.c` 文件**（`#include` 展开后）单独编译 → **`.o`**
- **链接器** 合并 `.o`；**外部链接** 符号跨文件 **定义一次、引用多处**（[4.4 §3](./4.4-作用域规则.md)）

§2.1 的 `global_num` 例子走的就是这条路径：`common.h` 保证各 `.c` **编译时声明一致**；链接器保证 **运行时只有一份实体**。

### 4. 单定义规则：`.h` 只声明，`.c` 定义

| 放在 `.h` | 放在 **某一个** `.c` |
|-----------|---------------------|
| `extern int num;` | `int num = 0;` |
| `int foo(int x);` | `int foo(int x) { return x; }` |
| `struct point { int x, y; };` | （类型定义可放 `.h`；带静态数据的定义放 `.c`） |

- 每个 **外部链接** 符号全程序 **只能定义一次**
- **`.h` 被多个 `.c` include** 时，写 `int num = 0;` → 每个 `.c` 各一份定义 → 链接 **重复定义**
- 正确：`.h` 写 **`extern int num;`**，**一个** `.c` 写 **`int num = 0;`**

见 [4.3](./4.3-外部变量.md)。

### 5. include 守卫（防止重复展开）

```c
#ifndef MATH_UTILS_H
#define MATH_UTILS_H
/* 头文件内容 */
#endif
```

同一 `.c` 多次 include 或链式 include 时，避免重复定义类型/宏。

### 6. `#include "..."` vs `#include <...>`

| 写法 | 查找路径 | 典型 |
|------|----------|------|
| **`#include "foo.h"`** | **当前目录**、工程路径优先 | 项目自己的 `.h` |
| **`#include <stdio.h>`** | **系统/编译器** 标准路径 | 标准库、第三方库 |

### 7. 组织方式：头文件 **不必** 与 `.c` 一一对应

**没有**「每个 `.c` 必须配一个同名 `.h`」的硬性规定。怎么拆头文件，**完全看项目需求** —— **怎么方便维护就怎么来**。

| 做法 | 适用场景 | 示例 |
|------|----------|------|
| **公共头文件** | 多个 `.c` **共用** 的声明、类型、宏 | `common.h` 放 `extern int global_num;`，`a.c` / `b.c` / `c.c` 都 include |
| **模块头文件**（常与 `.c` 成对） | 某个 `.c` 实现一个模块，对外暴露 **API** | `stack.h` 声明 `push`/`pop`，`stack.c` 实现；`main.c` 只 include `stack.h` |
| **混合** | 真实工程最常见 | 全局配置/错误码放 `common.h`；各子系统各有 `xxx.h` + `xxx.c` |

```
方式 A —— 公共头：
  common.h  ←── a.c, b.c, c.c 都 include（共享 extern、宏、类型）

方式 B —— 模块头：
  stack.h  ←── stack.c（实现）+ main.c（调用方）
  parser.h ←── parser.c + main.c

方式 C —— 混合：
  common.h + stack.h + parser.h + main.c
```

**原则**：

- **谁需要这份声明，谁就 `#include` 对应 `.h`** —— 不必为了「对称」硬造没用到的头文件
- **`.h` 描述的是「接口/契约」**，不是「`.c` 的镜像」；一个 `.h` 可被很多 `.c` include，一个 `.c` 也可 include 多个 `.h`
- 小项目：`common.h` 一把梭；变大后：按模块拆 `stack.h`、`io.h` 等，**边界更清晰**

下面栈模块示例是 **方式 B**（`stack.h` + `stack.c`）；§2.1 的 `global_num` 是 **方式 A**（`common.h` 跨多文件）。

## 示例：栈模块（K&R 风格）

```c
/* stack.h —— 声明集中处 */
#ifndef STACK_H
#define STACK_H

#define MAXVAL 100
void push(double f);
double pop(void);

#endif
```

```c
/* stack.c —— 实现 + 本文件私有 */
#include "stack.h"

static int sp = 0;           /* static：不放进 .h，仅本 .c */
static double val[MAXVAL];

void push(double f) { /* ... */ }
double pop(void)    { /* ... */ }
```

```c
/* main.c */
#include "stack.h"

int main(void)
{
    push(1.0);
    return 0;
}
```

## 易混点

| # | 要点 |
|---|------|
| 1 | **`.h` = 声明**；**`.c` = 定义/实现** |
| 2 | 多 `.c` 共用声明 → 写 **一个 `.h`** + **`#include`** |
| 3 | `.h` 里 **`extern` 声明**，**一个** `.c` **定义** |
| 4 | `.h` 里写 `int x = 0;` → 多 include → **链接炸** |
| 5 | **`static` 私有** 放 `.c`，**不要** 写进对外 `.h` |
| 6 | include 守卫防重复展开 |
| 7 | **`.h` 不必与 `.c` 一一对应** —— 可 `common.h` 共享，也可每模块一对，按维护性取舍 |

## HFT / 内核提示

- 驱动：**`xxx.h` 对外 API**，**`xxx.c` 实现** + `static` 私有
- 策略引擎：公共类型/错误码放 `.h`，热路径实现藏 `.c`
- 改 `.h` 中结构体布局 → **所有 include 的 `.c` 重编**（接口稳定性）

## 与后续衔接

| 章节 | 内容 |
|------|------|
| **4.8** | 逆波兰计算器多 `.c` + 各 `.h` |
| **4.11** | `#include` 预处理 |
| **ch06** | 结构体定义常放 `.h` |

## 面试题

1. 头文件主要放什么？为什么不把 `int num = 0;` 定义放 `.h`？
2. `b.c` 只 `#include "common.h"` 后能否直接 `printf("%d", global_num);`？**编译**时和**链接**时各发生什么？
3. `extern` 声明为什么适合放 `.h`？
4. `static` 函数/全局应放在 `.h` 还是 `.c`？为什么？
5. `#include "x.h"` 与 `#include <x.h>` 有何区别？
6. 把 `global_num` 从 `int` 改成 `float`，最少要改哪几处？为什么？
